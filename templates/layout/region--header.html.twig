
{%
  set classes = [
    'region',
    'region-' ~ region|clean_class,
  ]
%}
{% if content %}
  <div class='' >
    <div{{ attributes.addClass(classes) }}>
      {{ content }}
    </div>
  </div>
{% endif %}
